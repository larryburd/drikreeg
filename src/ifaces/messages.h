#include "game_state.h"

//void moveMsg(char* msg, int msgSize, int currPlayer, struct Piece* piece, int to_x, int to_y);
void moveMsg(char* mas, int msgSize, int currPlayer, struct Move* move, struct GameState* gs);
